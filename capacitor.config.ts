import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.alraqam.nasiya',
  appName: 'Nasiya 365',
  webDir: 'out',
  server: {
    "url": "http://localhost:3000"
  }
}

export default config
